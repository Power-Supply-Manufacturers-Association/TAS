{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://psma.com/tas/utils.json",
  "title": "TAS utils",
  "description": "Shared primitive types used across TAS schemas.",
  "$defs": {
    "dimensionWithTolerance": {
      "type": "object",
      "additionalProperties": false,
      "description": "A scalar value expressed as nominal and/or a [minimum, maximum] range. At least one of nominal/minimum/maximum must be present.",
      "properties": {
        "nominal": { "type": "number" },
        "minimum": { "type": "number" },
        "maximum": { "type": "number" }
      },
      "anyOf": [
        { "required": ["nominal"] },
        { "required": ["minimum"] },
        { "required": ["maximum"] }
      ]
    }
  }
}
